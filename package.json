{
  "name": "driftgate-rts",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "scripts": {
    "build": "cd driftgate_extracted && npm install && npm run build && :",
    "dev": "cd driftgate_extracted && npm install && npm run dev",
    "preview": "cd driftgate_extracted && npm run preview"
  },
  "dependencies": {
    "@base44/sdk": "^0.8.41",
    "@base44/vite-plugin": "^1.0.30",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^6.1.0",
    "@vitejs/plugin-react": "^4.3.4"
  }
}